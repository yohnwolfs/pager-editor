/**
 * 配置文件
 */
const config = {
  /** 部署路径 */
  publicPath: '/pager-editor',
  /** 视窗分割列数 */
  viewPortCols: 100,
  /** 视窗每行高度 */
  viewPortRowHeight: 2,
  /** material版本 */
  baseMaterialConfig: { version: '0.0.1' },
  /** 默认新组件宽度 */
  defaultNewItemWidth: 100,
  /** 默认新组件高度 */
  defaultNewItemHeight: 20,
  /** 默认页面视窗宽度 */
  defaultViewPortWidth: 320,
  /** 默认页面视窗高度 */
  defaultViewPortHeight: 500,
};

export default config;
